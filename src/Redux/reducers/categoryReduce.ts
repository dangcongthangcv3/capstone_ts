import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const categoryReduce = createSlice({
  name: 'categoryReduce',
  initialState,
  reducers: {}
});

export const {} = categoryReduce.actions

export default categoryReduce.reducer